[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "atomic-ai-engine"
version = "0.1.0"
description = "Atomic AI capability engine SDK"
requires-python = ">=3.10"
